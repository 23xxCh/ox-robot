# PM after episodes L00 / L02 / L01

Product assumptions (no audience interviews).

Three candidate slices:
1. NIU-L03 — health/version + ASR/TTS fail does not replay old secret audio (unlocks BE observability; no live uvicorn kill).
2. P-04 — rehearsal page stops showing fake completed device state (FE-1/2).
3. P-02 — interrupted secret used once then consumed (EX-4; MemoryStore already partial).

Chosen next: **NIU-L03**. Why: after freeze/isolation/rhythm, the demo still cannot tell whether the running brain is the new code; abort/fail recovery is a safety-adjacent software gap that does not need flash. P-04/P-02 wait one more slice.

Non-goals this slice: restart PID 41712, flash, ClawBot token, Feishu.
