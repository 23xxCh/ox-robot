# 牛来：实体对话基础连通通过

**11:31 最新结论：用户确认“对话通了”，串口同时记录了实体麦克风转录、连续回答，以及一次真正的“牛来”关键词唤醒闭环。当前可用固件已保存在私有忽略目录，保留运行版本。唤醒成功率、长时间稳定性和低延迟门禁尚未通过。**

2026-09-06。用户已确认接线及台架准备，并授权继续开发和真机测试。本记录覆盖第三、四轮；第二轮文档中的“尚未烧录”是历史状态。

## 已观察到的问题

1. BOOT 能播放本地“妈妈”，但测距 PRESENT/UNKNOWN 抖动调用同一个取消处理，撤销了播完后的联网收音。串口记录见 `.scratch/codex-round3/serial-live.log`。
2. 开机时测距回调提前启用语音引擎，模型还没从 assets 加载，实际日志为 `detector: none`。联网后模型加载成功却被音频引擎拒绝替换，导致喊名字无法唤醒。先后顺序见 `.scratch/codex-round4/boot-before.log`。

## 修复约束

有人或测距未知时始终停腿、停止独处行为；已由 BOOT/按键/唤醒发起的礼貌对话继续。显式取消、断网和协议关闭仍取消会话。

牛来在启动音频任务前加载随固件烧录的本地资源。使用既有资产检查标记跳过本轮联网下载和重复 Apply，避免释放引擎正在使用的模型；资源更新需随固件重新构建和烧录。其他板型保留原流程。加载成功与实际唤醒检测成功分别验收。

## 第三轮已完成

- 后端 WS 握手校验独立设备 Bearer 密钥；IM 另用独立密钥；控制界面只允许本机真实来源，禁用代理头对来源的改写。真实 TCP 验证见 `.scratch/codex-round3/network-verification.json`。
- 后端测试由独立代理执行：181 passed、1 warning，15.84 秒。该次终端结果没有保存完整原始日志，不宣称已有不可变测试快照。
- 本地服务地址 `http://127.0.0.1:8000/life`；设备连接 `ws://192.168.18.144:8000/xiaozhi/v1/`。这是本地 Wi-Fi 网络，当前服务版本 `codex-285c2285f0bf`。
- 已备份受影响 Flash 区域并完成 COM6 烧录，五个写入区域均通过 esptool 哈希验证。第三轮应用 SHA256：`fff5a299f85842c8afaf21dca93de77103897754201ca3d584d54044e303eeef`。日志和含私有配置的备份留在忽略目录 `.scratch/codex-round3/`。
- 真机启动取得地址 `192.168.18.251`；本地“妈妈”能播放。尚不能据此断言实体 ASR、TTS 和连续对话通过。

## 验收边界

既有云端 TTS → Opus → ASR 合成输入测试通过，电脑文字对话返回真实 LLM 输出。修复后已补充完整音频 WebSocket 和实体麦克风问答正例，用户确认听到回答。合成输入、目标板状态、用户听感各自记录，不互相替代。健康接口的 `firmware_runtime_verified: false` 仍是软件元数据，不能用它证明板上版本或体验。

真机日志 `.scratch/codex-round4/serial-after.log`：BOOT 在 263078 ms 触发妈妈；287748 ms 转录“你是谁？”，288748 ms 收到“你好呀，我是桌上的小牛摆件，叫我牛来就好啦！”并进入 Speaking，随后回 Listening。多个测距 PRESENT/UNKNOWN 更新保留了这次礼貌会话。

387368 ms，模型实际识别 `niulai`，置信值 0.177968，应用随即记录 `Niulai polite chat for wake '牛来'` 并播放妈妈；395568 ms 进入 Listening，403128 ms 转录“你可以做什么？”，404168 ms 收到回答并进入 Speaking。用户此前确有喊名字无反应的反馈，此处只记录一次成功，不推算成功率。Listening 状态按既有配置关闭重复关键词唤醒，此时可直接继续对话。

当前固件 `.scratch/codex-round4/working-dialogue.bin` SHA256 `2fa63f2a6ff446d013f48b4bbf83476df73034289da482f9ed7a28f3eb279c46`。串口已释放且未重置设备，服务继续运行。七项质量门禁仍需各自按既定分母与时序标准验收，本轮不直接升为 PASS。

## 当前服务的完整语音链路

11:22 使用真实云 TTS 生成“你好牛来，你是谁？”，编码为 38 个实际 16 kHz 单声道、60 ms Opus 包，按实际节奏上传到正在运行且要求设备鉴权的 WebSocket。ASR 返回“你好牛，来，你是谁？”，回答文本为“你好呀，我是桌上的小牛摆件，乖乖的，会一直陪着你的～”。服务器返回 83 个 Opus 包，成功解码为 4.9735 秒音频，再次送 ASR 得到相符的回答内容。连接已释放。

一次样本中，从停止上传起：识别文本 1.287 秒、回答文本 2.112 秒、首段音频 4.074 秒、播放发送结束 9.001 秒。它证明当前服务软件链路能完成语音问答，尚未达到低延迟体验验收；输入是合成语音。协议未报告回答使用的具体 LLM/TTS 提供方，不能单从返回帧排除云端/本地回退。

[验证结果](../outputs/niulai-dialogue-check-2026-09-06/result.json) · [回答音频](../outputs/niulai-dialogue-check-2026-09-06/reply.wav)。原始脚本与完整证据在 `.scratch/codex-round4/cloud-pipeline/`。

## SEC-2 增量评分

独立复评仅将 SEC-2“来源与授权”从 0 调至 2：设备与 IM 密钥分离、入口拒绝和本机来源约束已有源码、25 个边界用例及真实 TCP 记录支持。安全维度由 25 派生为 35，总分由 30.75 派生为 **31.75/100**。其余 39 项沿用 2026-09-06 10:14 的旧快照，这不是整项目重审；原 7 个门禁仍为 NOT_RUN，不因鉴权修复或烧录提升。目标板鉴权连通仍待验证；181 项通过缺完整原始日志，不作为当前版本的不可变验证快照。版本哈希、证据哈希及计算见 [SEC-2 增量记录](../.grok-loop/scores/20260906-auth-delta.json)。
