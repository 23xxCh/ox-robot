# 牛来机器人非功能与安全需求分析

日期：2026-08-31  
输入：`bmad/context/architecture-scope.md`  
阶段：Requirements Analysis  
状态：供系统架构综合使用；本文识别需求与架构约束，不作最终技术选型或架构决策

## 1. 分析口径

- **已确认**：范围基线中已有明确指标或行为边界，可直接作为验收条件。
- **推导约束**：为满足已确认行为而必需，但具体阈值仍需实测或团队确认。
- **建议指标**：为了让需求可测试而提出的候选阈值，不应在未确认前视为冻结需求。
- **P0**：比赛演示失败、安全失效或人格穿帮时不可接受。
- **P1**：明显影响稳定性、调试效率或体验，但存在比赛现场降级路径。

本文将“传感器发现事件的延迟”和“系统收到事件后的控制链路延迟”分开测量。已确认的 300 ms 只覆盖后者，不能用它替代雷达、超声波自身的检测延迟和机械制动距离。

## 2. 架构驱动摘要

| 驱动 | 来源与强度 | 对后续架构设计的约束 |
|---|---|---|
| AD-01 确定性安全控制 | P0、已确认 | 人员状态、急停、障碍、低电和执行器超时必须拥有高于 Agent、Lua、LLM 的裁决权；安全路径不能依赖网络或生成式模型。 |
| AD-02 断网仍完成核心演示 | P0、已确认 | 本地必须具备状态判断、停车、基础移动、眼嘴头动作、固定“妈妈”音频和本地吐槽兜底；云端只能是可降级增强项。 |
| AD-03 300 ms 控制链路上限 | P0、已确认 | 事件分发、任务优先级、执行器命令撤销和日志时间戳必须可测；阻塞式音频、网络或脚本不能占用安全控制路径。 |
| AD-04 硬件型号和到货时间不确定 | P0、已确认 | 人员检测、唤醒、显示、电机驱动等需有稳定接口以及 Mock/按钮/替换驱动；选型不得把安全状态机绑定到单一器件协议。 |
| AD-05 移动硬壳、重量与重心未知 | P0、已确认 | 最终外壳前必须进行带假负载的驱动、急停、防倾和热测试；电机、舵机、电池及结构接口需等实测参数才能冻结。 |
| AD-06 三人短周期并行开发 | P0、已确认 | 软件、电子、机械应能通过接口和模拟器独立验证；构建版本、接口契约、测试记录和校准参数需要可复现。 |
| AD-07 人格内容与环境隐私边界 | P0、已确认/部分未决 | 生成文本必须受内容边界和安全回退约束；P0 不采集摄像、照片和多用户身份，但麦克风数据是否上传、保存多久仍需确认。 |
| AD-08 可诊断的现场 Demo | P0、已确认 | 状态切换、停车原因、传感器状态、电量、Agent 超时必须形成带时间信息的结构化记录，故障后能快速判断是感知、控制、供电还是 Agent 问题。 |

## 3. NFR 明细矩阵

### 3.1 实时性与性能

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-RT-001 | P0 / 已确认 | 从系统收到 `PRESENT` 或 `UNKNOWN` 事件，到现有运动命令被撤销并进入停车流程，端到端延迟 **≤300 ms**。雷达检测延迟单独报告。 | 安全事件需抢占普通动作；事件、裁决、命令撤销及驱动接收点需要统一时间基准和可追踪记录；网络、TTS、LLM、Lua 不得阻塞此链路。 | 在轮子离地和带假负载两种工况下，注入 100 次 `PRESENT`、100 次 `UNKNOWN`；用 GPIO 标记/逻辑分析仪或高帧率录像测量 P50/P95/最大值，要求最大值不超标。 | “进入停车流程”是否等于 PWM 归零、制动命令发出还是轮速为零；机械完全停止时间和停止距离上限尚未冻结。 |
| NFR-RT-002 | P0 / 推导约束 | 实体急停触发后必须优先于全部软件动作使驱动进入安全态。**建议指标：触点动作到电机失能/制动命令生效 ≤100 ms**，最终阈值待电气方案确认。 | 急停检测与执行路径不得依赖 Agent 调度；需明确硬件断能和软件急停的职责边界，并能验证触发后不可被普通命令重新使能。 | 重复触发/复位各 50 次，覆盖前进、转弯、播音、舵机动作和 Agent 卡死场景；示波器测量急停信号与驱动使能/PWM。 | 急停采用直接切断电机电源、驱动 `STBY/EN` 还是两级联锁；复位是否要求人工二次确认。 |
| NFR-RT-003 | P0 / 推导约束 | 障碍事件必须在危险接近前取消前进动作。验收应同时规定“有效测距到撤销命令的延迟”和“最高演示速度下的总停止距离”；具体数值待速度、轮径、重量实测后冻结。 | 超声采样、异常值处理、速度上限和停车逻辑需处于确定性控制路径；需要近距离盲区和测距失效的保守策略。 | 在不同距离、角度和材质障碍物前进行不少于 30 次接近试验；记录测距、事件、命令撤销、轮速归零和最小剩余距离。 | 演示最高速度、障碍阈值、HC-SR04 壳体/鼻孔造成的声学影响、地面摩擦系数。 |
| NFR-RT-004 | P1 / 建议指标 | `WAKE_NIULAI` 有效事件到本地“妈妈”音频开始播放的延迟建议 **≤500 ms**；嘴部首个动作与音频起始偏差建议 **≤150 ms**。按钮备份与 KWS 应产生同一事件语义。 | 音频、嘴、眼、头动作需通过同一动作编排时间线触发，但不能形成阻塞安全控制的长事务。 | 按钮和本地 KWS 各触发 30 次，录制音视频逐帧测量；同时注入 `PRESENT`/急停验证编排可被取消。 | KWS 实际算法及误唤醒/漏唤醒目标；“妈妈”音频长度；动作同步容差是否影响现场体验。 |
| NFR-PERF-001 | P0 / 推导约束 | 安全任务在最坏并发情况下不得饥饿：网络超时、TTS、日志写入、OLED 刷新、舵机和电机同时运行时，NFR-RT-001 仍必须成立。 | 需隔离实时安全工作与非实时工作，限制阻塞调用、队列深度和单次执行预算；资源耗尽时优先丢弃/降级非安全工作。 | 构造“最坏负载”压力场景，连续运行至少 30 分钟并重复人员事件；记录任务运行时间、队列水位、内存最低余量和 300 ms 指标。 | ESP32-S3 实际 PSRAM/堆使用预算、各任务周期及优先级、音频和屏幕驱动峰值占用。 |
| NFR-PERF-002 | P1 / 推导约束 | 三分钟演示期间不得因内存持续增长、队列积压或存储写阻塞导致动作卡顿；10 次连续 Demo 后关键资源应回到稳定区间。 | 需要有界队列、固定上限的文本/音频/日志缓存、Agent/Lua 执行超时和可观测的资源水位。 | 连续执行 10 次完整 Demo，比较每轮前后的可用堆、最大连续块、队列峰值和任务看门狗；检查是否存在单调恶化。 | 可接受的最低空闲堆、碎片率和队列上限需在固件成型后基准化。 |

### 3.2 功能安全、电气安全与执行器约束

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-SAFE-001 | P0 / 已确认 | 裁决优先级固定为：**实体急停 > 人员 `PRESENT/UNKNOWN` > 障碍 > 低电量 > 正常动作 > LLM 意图**。任意低优先级命令不得覆盖高优先级安全状态。 | 需要单一、可审计的安全裁决边界；所有执行器命令必须经过该边界；状态与优先级规则不能散落在脚本和驱动中。 | 对每一对有冲突的优先级事件生成组合测试，至少覆盖同时到达、乱序到达、重复事件和状态恢复；断言最终执行器状态和停车原因。 | 高优先级状态解除条件、急停复位流程、低电恢复后能否自动重新移动。 |
| NFR-SAFE-002 | P0 / 已确认 | LLM 与 Lua 只可请求白名单语义动作；不得直接设置 PWM、原始轮速、任意舵机角度或无限时长动作。所有动作必须具备幅度、速度、持续时间和可取消上限。 | 语义动作接口需执行参数校验、限幅、超时和状态授权；生成内容和脚本均视为不可信输入。 | 模糊测试越界角度、负时长、超长动作、NaN/溢出、未知动作和并发动作；要求全部拒绝或钳制且留下原因日志。 | 每个动作的角度、速度、加速度、持续时间上限；是否允许 Lua 组合并发动作。 |
| NFR-SAFE-003 | P0 / 已确认 | 上电、重启、断网、Agent 超时、脚本崩溃、传感器故障均默认停车；人员状态初始化、超时或无效时为 `SAFE_UNKNOWN`。 | 所有输入需具备 freshness/健康状态；启动默认输出安全；看门狗和故障传播不得把未知状态解释为无人。 | 逐项执行断电重启、拔网线/禁 Wi-Fi、停止 Agent、制造脚本异常、断开雷达和注入乱码；每项重复 10 次，检查执行器、人格和日志。 | 传感器新鲜度超时阈值、Agent 心跳阈值、脚本错误后的自动恢复策略。 |
| NFR-SAFE-004 | P0 / 推导约束 | 舵机和电机的机械/电气边界不得仅依赖生成动作；头、嘴需有软限位并配合机械限位，卡滞时不得长期堵转。 | 执行器层需要校准值、方向、零点、限位和最大连续动作时间；结构应允许限位与线缆余量检查。 | 在装壳前对每个执行器执行边界动作 100 次；人为阻挡验证超时/过流/温升处置；检查线缆不拉扯、不夹伤。 | 头部实际质量/力臂、舵机型号和扭矩、是否有电流检测、允许表面和绕组温升。 |
| NFR-SAFE-005 | P0 / 推导约束 | 低电量必须先停止移动，再降级非关键负载，避免 MCU 棕断或锂电过放。低电阈值、迟滞和关机电压必须依据最终电池/BMS 数据表冻结。 | 供电监测需进入安全裁决；电机/舵机瞬态压降不能被误判为稳定电量，低电流程必须可重复且不可振荡。 | 可编程电源扫压和带负载放电；验证预警、停车、降级、关断顺序以及阈值迟滞，覆盖电机启动电流。 | 电池化学体系、串数、BMS、充电器、分压精度、低电阈值和是否具备硬件欠压保护。 |
| NFR-SAFE-006 | P0 / 推导约束 | 电源系统在电机堵转、舵机启动和音频峰值同时发生时不得过流失控、线材过热或使控制器复位。 | 需要按实测峰值留出电流和散热裕量，明确保险/过流保护、线径、接插件额定值以及不同负载域之间的干扰隔离需求。 | 测量正常、启动、转向、堵转和复合负载电流；持续热测试并监测各降压模块、驱动、线材、接头及电池温度；人为短时堵转验证保护。 | 可接受温升、堵转测试时长、驱动芯片、电源拓扑、保险类型和额定值。 |

### 3.3 可靠性、可用性与降级

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-REL-001 | P0 / 已确认 | 连续完成完整三分钟 Demo **至少 10 次**，无死机、撞击、倾倒或人格穿帮。 | 核心路径必须短小、状态确定、资源有界；每轮演示应能从已知初始状态开始，并能清除上轮的临时动作。 | 使用冻结脚本完成 10 轮连续演示并录像；每轮检查状态序列、停车原因、资源水位和物理结果，失败即重新计数。 | 两轮之间是否允许充电/重启；“人格穿帮”的可观察判据需写成测试脚本。 |
| NFR-REL-002 | P0 / 已确认 | 无网络时仍可完成：人员检测、人格切换、移动/停止、眼睛、头、嘴和本地语音。 | 云端 LLM/TTS 必须是非关键依赖；超时应快速失败并切换本地语料/固定音频，不能阻塞动作或积压请求。 | 在演示开始前禁用网络，执行 10 轮完整流程；演示中途断网再执行 10 次，确认无长时间静默、卡死或错误人格。 | 云端请求超时值、是否缓存云端结果、本地吐槽语料规模和选择规则。 |
| NFR-REL-003 | P0 / 推导约束 | 单个非安全模块故障不得让安全控制失效：OLED、音频、LLM、Lua 或日志失败时仍必须停车和响应急停。 | 需要故障隔离、超时和降级边界；安全路径不得等待非关键模块完成。 | 分别断开/禁用各模块并注入异常返回，执行人员事件与急停；记录其余能力和停止延迟。 | 哪些模块失败时允许继续移动，哪些必须整机停车；屏幕或音频失败是否算 Demo 失败。 |
| NFR-REL-004 | P0 / 推导约束 | 重复或乱序的传感器/Agent 事件不得造成状态抖动、恢复旧动作或重复执行危险动作。 | 事件需要时间戳/序号、新鲜度和幂等语义；恢复动作必须基于当前裁决状态，而不是旧队列。 | 注入重复、延迟、乱序和突发事件；检查状态转移、动作队列与最终安全状态，特别验证 `UNKNOWN` 不会被旧 `ABSENT` 覆盖。 | 是否存在跨重启持久事件；事件队列容量和丢弃策略。 |
| NFR-REL-005 | P1 / 建议指标 | 严重软件故障发生后，系统应在可接受时间内恢复到“停车且可诊断”状态。建议看门狗恢复或人工重启后 **≤10 s** 到达 `SAFE_UNKNOWN`，但不得自动恢复自由移动。 | 启动自检、持久化最小故障原因和安全初始化顺序需要明确；恢复与重新授权应分离。 | 注入死循环、任务阻塞和异常重启，测量到达安全状态时间并验证最后故障原因可读取。 | 是否启用硬件/任务看门狗、重启次数上限、反复重启后的锁定策略。 |

### 3.4 供电、续航与热设计

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-PWR-001 | P0 / 推导约束 | 电池至少支持 10×3 分钟完整 Demo 加必要的启动和场间等待，且最后一轮仍不触发非预期低电停车。最终“连续续航分钟数”尚未在范围基线冻结。 | 电池容量应由代表性工况的平均/峰值功耗和安全余量反推；不能仅按标称容量估算。 | 用最终假负载和演示脚本进行完整放电测试，记录电压、电流、温度与每轮剩余容量。 | 是否采用 60–90 分钟目标、场间是否充电、待机时长、允许电池质量和空间。 |
| NFR-PWR-002 | P0 / 推导约束 | 最大复合负载下 ESP32-S3 不得发生棕断、异常复位或音频/雷达失真；各电源轨应保持在器件数据表容差内。 | 需要测量电源轨动态响应、地回路和电机噪声耦合；供电域、滤波和储能元件需依据实测决定。 | 示波器监测 MCU、舵机、音频和电机电源轨，重复电机启停/反转及舵机同步启动至少 100 次。 | 各模块工作电压、降压模块持续/峰值能力、是否分域供电、允许纹波值。 |
| NFR-PWR-003 | P0 / 推导约束 | 充电、运输和维护不得暴露无保护电芯或可短路端子；电池必须可固定、可拆且不会因急停惯性位移。 | 结构和电气需共同定义电池仓、应力释放、反接防护、保护板与充电边界；维护门不能让用户直接碰到危险裸露导体。 | 检查电池固定和接插件防呆；进行急停/轻微晃动后复检；用错误插接和短路风险清单做桌面审查。 | 电池包供应商与认证状态、BMS/充电方案、比赛场地对锂电池的规则。 |
| NFR-PWR-004 | P1 / 建议指标 | 30 分钟代表性连续运行后，电源模块、电机驱动、舵机和电池温升不得超过各自数据表允许值，并应保留安全余量。 | 需要温度监测点、通风/隔热和热源与塑料件/电池的间距设计；具体风道不能在器件损耗未知时冻结。 | 在最终壳体或等效封闭条件下运行代表性动作 30 分钟，用热电偶/热像记录热点，随后执行一次完整 Demo。 | 室温基线、材料耐温、允许表面温度和各器件降额曲线。 |

### 3.5 机械稳定性、结构可靠性与可维护性

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-MECH-001 | P0 / 已确认 | 在最终重量假负载和正式外壳下完成移动、转向、急停及 10 轮 Demo，不倾倒。 | 低重心、电池下置、轮距、轴距、后滚珠和前防倾支点是整机安全约束；外观曲面不得决定承力和重心。 | 建立等重等重心假负载；覆盖直行急停、原地转向、最大允许速度转弯和轻微地面接缝，各至少 20 次；记录倾角和防倾支点是否触地。 | 最终总质量、重心高度、最高速度/加速度、轮距/轴距、允许地面坡度和接缝高度。 |
| NFR-MECH-002 | P0 / 推导约束 | 静态状态下重心投影应位于支撑多边形内并留有工程余量；动态安全余量需通过急停测试确认。**建议静态边缘余量 ≥15 mm**，待总布置完成后确认。 | 电池、驱动、电源模块和头部重量需进入统一质量模型；头部两轴运动要评估最不利姿态。 | FreeCAD 质量属性估算 + 实物四角秤/悬挂法复测；分别测量头部极限姿态下的重心位置。 | 3D 打印件真实密度、线材/紧固件质量、头部舵机和屏幕质量。 |
| NFR-MECH-003 | P0 / 已确认 | 混元/群核 STL 只负责外观；承力、孔位、卡扣、螺丝柱、电机/舵机座及分件接口必须由参数化工程骨架定义。分件须适配 330×320×325 mm 打印空间。 | 外观网格与工程骨架需要清晰装配基准；关键安装面不能依赖不可参数化的生成网格；需定义打印方向、装配公差和可更换件边界。 | 对每个打印件做包络检查、干涉检查和装配检查；先打印关键接口试片，再打印整件；至少完成一次无破坏拆装。 | 喷嘴直径、材料、层高、热缩/孔径补偿、螺丝/热熔螺母规格和卡扣寿命。 |
| NFR-MECH-004 | P0 / 已确认/推导 | 背部检修门和下部可拆电池应允许不破坏外壳地更换电池、接触急停/主电源并检查主要接插件。建议单人使用常用工具在 **≤5 分钟** 内完成电池更换。 | 线束需要接插件、应力释放和服务余量；易损模块和校准点应可访问，不能被粘死在外壳内部。 | 按维护清单计时完成开门、断电、换电、复装和通电自检，重复 5 次，检查线束和螺纹无损。 | 是否允许磁吸件、维护所需工具、检修门锁止方式和比赛运输要求。 |
| NFR-MECH-005 | P1 / 推导约束 | 扬声器音腔、耳部麦克风、毫米波和鼻孔超声的结构集成不得使传感与音频性能下降到不可演示。 | 结构需要为声学隔离、非金属雷达窗、超声开孔以及电机振动隔离保留边界；这些边界需通过样机实测迭代。 | 装壳前后对比 KWS/录音信噪比、雷达检测范围/误报、超声有效距离和扬声器失真；在电机运动时重复测试。 | 壳厚和材料、雷达型号/FOV、麦克风灵敏度、目标声压和允许误唤醒率。 |

### 3.6 可维护性与可替换性

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-MAINT-001 | P0 / 已确认 | 人员检测通过 `presence_read() -> PRESENT | ABSENT | UNKNOWN` 暴露稳定契约；Mock、按钮、GPIO 雷达和 UART 雷达可替换，且替换不得改写人格/安全状态机。 | 传感器协议适配与领域状态需分离；驱动必须报告健康状态和数据新鲜度，而不只是布尔值。 | 用同一组状态机测试依次运行四种驱动；替换驱动后安全测试不变且全部通过。 | GPIO/UART 雷达的实际帧格式、区域配置和健康判据。 |
| NFR-MAINT-002 | P0 / 推导约束 | 电机、显示、KWS、音频和 Agent 能力也应有明确接口与 Mock；硬件未到场时三名成员仍能并行开发并跑通核心状态序列。 | 模块边界需围绕能力而非芯片型号；每个接口要定义成功、超时、不可用和取消语义。 | 在无实际雷达、显示和云端服务条件下，用 Mock 执行完整 3 分钟脚本；再逐个换成实物驱动进行契约测试。 | 需要 Mock 的最小模块集合、桌面模拟器形式及团队分工。 |
| NFR-MAINT-003 | P0 / 已确认/推导 | ESP-Claw、ESP-IDF 与依赖版本必须被固定，并能从干净环境复现构建。建议一条文档化命令完成配置与编译，构建产物记录 commit 和配置摘要。 | 依赖、补丁、板级配置和凭据需要分离；不能依赖某位成员机器上的隐式环境。 | 在第二台电脑或干净环境按 README 构建；比较固件版本信息和关键配置，要求无手工改源码步骤。 | CI 是否可用、工具链缓存方式、ESP-Claw 上游 commit 和补丁维护方式。 |
| NFR-MAINT-004 | P1 / 建议指标 | 修改一个硬件适配器或校准参数，不应要求修改安全裁决、人格状态机和无关驱动；关键接口变更必须同步更新测试和文档。 | 需要小而清晰的模块边界、配置版本和架构决策记录；避免比赛期引入微服务或重复固件等不必要复杂度。 | 选取雷达或显示作为替换演练，统计受影响文件/模块；运行完整回归并检查接口文档。 | 允许的变更范围指标、配置存储位置和版本迁移策略。 |

### 3.7 可测试性与验收可重复性

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-TEST-001 | P0 / 推导约束 | 四个人格/安全状态及所有安全关键转移必须具备自动化测试；至少覆盖正常、超时、故障、重复、乱序和抢占。 | 状态机需可在无硬件条件下注入时间和事件；安全策略不应与真实 GPIO 直接耦合。 | 建立状态转移表并逐项映射测试；要求安全关键转移覆盖率 100%，代码行覆盖率不作为唯一判据。 | 完整转移表、超时值、恢复事件与并发事件规则。 |
| NFR-TEST-002 | P0 / 已确认 | 硬件未到场时，Mock、按钮和桌面模拟器必须能够执行核心 Demo；到货后使用相同事件契约进行 HIL/实机验证。 | 时间、传感器、网络、Agent 和执行器反馈需要可注入；测试夹具应与生产逻辑共享接口而不是旁路安全层。 | CI/桌面执行离线场景测试；实机阶段逐个替换适配器，比较事件和状态日志。 | 桌面模拟器技术形态、HIL 接线和是否需要 PC 端串口脚本。 |
| NFR-TEST-003 | P0 / 已确认/推导 | 上电、断网、传感器断开、Agent 卡死、低电和急停六类故障都必须有可重复的故障注入步骤，并验证确定性安全状态。 | 故障注入钩子、看门狗、供电测试点和结构化日志需在设计期预留；不能等整机封装后再考虑。 | 每类故障至少重复 10 次，记录期望状态、实际状态、反应时间和恢复步骤；形成通过/失败报告。 | 低电模拟方法、Agent 卡死注入方式、传感器断开的电气安全做法。 |
| NFR-TEST-004 | P0 / 推导约束 | 每项 NFR 应能追溯到至少一个测试用例和一份证据（日志、波形、录像、照片或测量表）。 | 需求 ID、日志事件和测试编号需统一；现场版本必须关联到固件 commit、结构版本和 BOM 版本。 | 建立 NFR—测试—证据矩阵，演示冻结前检查所有 P0 项无空白证据。 | 证据存放位置、命名规则和谁负责签字确认。 |

### 3.8 隐私、内容安全与网络安全

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-SEC-001 | P0 / 已确认 | P0 不启用 Insta360、视觉识别、照片记录、多用户身份识别和长期 SD 日记；系统不得暗中形成这些数据链路。 | 相机、图像存储和身份组件不应进入 P0 固件依赖；日志模式不应预留默认开启的原始个人数据采集。 | 检查 BOM、固件依赖、网络请求和存储内容；在演示期间抓取出站流量，确认无图像/身份数据。 | 麦克风音频是否上传到云端/PC，以及比赛现场是否需要显著提示。 |
| NFR-SEC-002 | P0 / 已确认/推导 | 无人吐槽允许轻度喜剧用语，但不得含脏话、仇恨、受保护特征攻击或危险诱导；生成失败或判断不确定时使用审核过的本地语料。 | 内容生成需要约束、输出审查/分类和确定性回退；安全控制不得解析生成文本来决定底层运动参数。 | 建立正常、挑衅、仇恨、危险指令及人物特征输入测试集；建议至少 100 条，要求禁止类别输出为 0，拦截时仍能给出本地安全台词。 | 中文边界词表、模型端与设备端各承担何种审查、误杀容忍度、是否允许提及现场人员外貌。 |
| NFR-SEC-003 | P0 / 推导约束 | 云端 LLM/TTS 输入遵循最小化原则：不发送不必要的原始音频、长期身份、完整日志或精确环境信息；日志不得记录 API 密钥和原始敏感内容。 | 云端请求需有字段白名单、脱敏和超时；秘密信息不得硬编码或进入版本库/串口日志。 | 代码/配置秘密扫描、出站请求审查和日志抽查；注入带密钥/敏感字段的错误，确认不会被打印。 | 云端供应商、数据保留政策、API 密钥存放方式、原始语音是否离开设备。 |
| NFR-SEC-004 | P0 / 推导约束 | 外部网络响应、LLM 文本和 Lua 脚本均视为不可信输入；畸形、超长或恶意输入不能越过白名单动作和资源限制。 | 需要输入长度限制、模式校验、超时、取消、内存配额和能力权限；失败时降级而非继续执行部分危险动作。 | 模糊测试 JSON/文本/脚本，覆盖超长输入、递归、未知字段、数值溢出和重复动作；检查无崩溃、无越权执行。 | Lua 是否允许动态下载/持久化、脚本签名或版本校验需求、最大文本与脚本尺寸。 |
| NFR-SEC-005 | P1 / 推导约束 | Wi-Fi/云端失陷不应使攻击者获得任意执行器控制；P0 无账户、量产 OTA 和对外管理 API 时，不引入超出 Demo 所需的远程入口。 | 网络能力应遵循最小权限和最小暴露面；远程数据只能转换为受限语义意图并再次经过本地安全裁决。 | 端口/服务清单审查、网络断开与恶意响应测试、凭据轮换演练；确认不存在未文档化远程控制通道。 | 是否需要 OTA、串口调试口的现场保护、TLS/证书能力和比赛 Wi-Fi 配置。 |

### 3.9 可观测性与现场诊断

| ID | 优先级 / 成熟度 | 需求与可测指标 | 架构影响 | 验证方法 | 未决项 |
|---|---|---|---|---|---|
| NFR-OBS-001 | P0 / 已确认 | 结构化日志至少记录：状态切换、停车原因、人员/障碍传感器状态、电量和 Agent 超时；每条安全关键记录必须含单调时间戳和事件/动作关联标识。 | 需要统一事件名称、原因码和时间基准；日志写入不得阻塞安全路径，队列满时优先保留安全事件。 | 对每种状态和停车原因各触发一次，自动检查字段完整性、顺序和关联性；压力下验证关键日志不丢失或能报告丢失计数。 | 日志格式、串口/内存/SD 去向、时间戳分辨率、队列容量和保留周期。 |
| NFR-OBS-002 | P0 / 建议指标 | Demo 故障后，团队应能在 **≤2 分钟** 内从日志判断首个异常属于感知、裁决、执行器、供电还是 Agent/网络，并看到最终停车原因。 | 需要少量高价值原因码、状态快照和启动/重启原因；避免打印大量无结构文本淹没关键事件。 | 使用预设故障但不告知诊断者，进行 10 次盲测并记录定位时间与正确率；目标正确率 100%。 | 2 分钟目标是否足够、是否需要 LED/屏幕现场诊断码、日志查看工具。 |
| NFR-OBS-003 | P0 / 推导约束 | 对 NFR-RT-001 的测量链必须能够区分：传感器检测、事件接收、安全裁决、命令撤销、驱动生效和机械停止六个时间点。 | 关键阶段需可打点，必要时预留 GPIO 测量脚或高分辨率本地时间；日志时间不得只用云端时钟。 | 用同一测试事件同时采集结构化日志、GPIO 波形和视频，校验各时间点可对齐。 | 可用 GPIO 数量、时间源精度、是否增加轮速/电流反馈。 |
| NFR-OBS-004 | P1 / 推导约束 | 资源与健康观测至少能报告：重启原因、空闲堆/最低堆、任务看门狗、事件队列峰值、传感器 freshness 和电源状态。 | 需建立轻量健康快照，避免引入不适合单机 ESP32 的重型监控系统；数据应可在现场导出。 | 30 分钟压力测试中定期采集健康快照，并通过已知故障验证指标变化符合预期。 | 采样周期、存储预算、现场导出方式和是否写入 SD。 |

## 4. 关键权衡与潜在冲突

| 冲突 | 风险 | 需要在架构阶段明确的取舍 |
|---|---|---|
| 有灵魂的自由行为 vs. 确定性安全 | 行为越自由，越难证明可取消、可限幅和不越权 | 自由度只能存在于语义意图和内容层，安全边界、速度、角度、区域、时长必须确定；具体动作集合待设计。 |
| 云端人格质量 vs. 离线可靠性/隐私 | 网络延迟、失败及数据上传可能破坏 Demo 或隐私 | 云端增强允许到什么程度、发送哪些字段、超时多少、何时回退本地，需要形成可测试策略。 |
| 硬壳外观还原 vs. 低重心、声学、雷达和维护 | 生成 STL 可能压缩内部空间、遮挡传感器或抬高头部重量 | 工程骨架和内部包络的优先级需高于外观网格；壳厚、窗口和分件基准待机械设计冻结。 |
| 更快移动 vs. TT 电机扭矩、停止距离与续航 | 速度增加会放大倾倒、碰撞、电流和热风险 | 必须先以假负载实测，再共同冻结最高速度、加速度、停止距离、电机/驱动和电池。 |
| 丰富日志 vs. 实时性、Flash/SD 寿命和隐私 | 同步写日志会阻塞安全任务，原始内容又可能包含敏感数据 | 日志等级、结构、缓存、丢弃策略和敏感字段边界需明确；不默认记录原始音频。 |
| 模块可替换 vs. 黑客松复杂度 | 抽象过多会拖慢实现，抽象不足又会被未知硬件卡住 | 只对已知易变边界做端口：人员雷达、唤醒、执行器、音频、显示、Agent/云端；不要引入与 P0 无关的平台化。 |

## 5. 阻塞最终架构冻结的未决参数

以下项目不阻塞接口和 Mock 开发，但会阻塞电气、机械和实时参数的最终冻结：

1. 整机目标重量、头部重量、重心高度、最高演示速度和允许停止距离。
2. TT 马达实际减速比、额定电压、正常/启动/堵转电流，以及是否需要更换更强减速电机。
3. 电机驱动持续/峰值电流与保护能力。
4. 电池化学体系、串数、容量、BMS、充电方式、比赛续航目标和场地锂电规则。
5. 头部和嘴部舵机型号、扭矩、力臂、限位与卡滞保护方式。
6. 24 GHz 雷达具体型号、GPIO/UART 协议、检测延迟、FOV、区域配置和传感器失效判据。
7. 2.42 寸 OLED 驱动芯片、接口、刷新时延和备件数量。
8. 本地 KWS 技术、误唤醒/漏唤醒要求、噪声条件及麦克风音频是否出设备。
9. 各电源轨峰值、纹波、热设计上限和允许表面温度。
10. Agent/Lua 心跳、超时、内存/队列预算，以及可接受的启动恢复时间。
11. 日志存储位置、容量、保留周期、现场查看方式和敏感字段规则。
12. 内容安全测试集、允许/禁止词边界和云端供应商数据保留政策。

## 6. 建议的 NFR 验证门（供主架构综合，不是最终排期）

| 验证门 | 进入条件 | 通过证据 |
|---|---|---|
| G0 接口与模拟安全门 | 无实物硬件也可构建 | 状态转移、优先级、超时、Mock/按钮、白名单动作和断网回退测试全部通过。 |
| G1 电源/滚动底盘安全门 | 电机、驱动、电源和急停到场 | 假负载电流/电压/热数据，急停与人员事件延迟，障碍停止距离，防倾测试记录。 |
| G2 头部/音频/显示台架门 | 舵机、OLED、麦克风、功放到场 | 限位/卡滞、动作同步、供电干扰、KWS 或按钮备份、装壳前后声学对比。 |
| G3 整机故障注入门 | 工程骨架和外壳装配完成 | 六类故障各 10 次、无倾倒、无越权、日志可在 2 分钟内定位原因。 |
| G4 Demo 冻结门 | 固件、结构、BOM 版本冻结 | 10×3 分钟完整演示录像及 NFR—测试—证据矩阵，所有 P0 项有结果。 |

## 7. 结论

本项目最强的架构驱动不是模型能力，而是“移动硬件的确定性安全 + 断网可演示 + 未知器件可替换 + 三人短周期可验证”。最终架构设计应逐条映射上述 P0 NFR，并明确所有建议指标是被团队确认、实测修订，还是保留为待决项；在电机、电池、雷达、舵机和重量数据到齐之前，不应宣称电源、机械稳定性或实时停止距离已经完成选型闭环。
