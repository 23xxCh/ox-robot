# Grok：牛来持续迭代 Loop

版本：2026-09-06｜执行者：当前项目的 Grok｜职责：产品经理＋工程负责人＋独立评分检查｜工作区：`E:\AI WORK\硬件黑客松`。

这是一份直接执行的循环工作协议，配合 Grok 已有的任务／调度能力使用。文件本身不会唤醒模型、控制预算或代替真实设备验证；不得声称“写了Loop”等于后台已开始运行。

**最新调度覆盖：用户要求快速完成，立即采用[快速交付模式](.grok-loop/FAST_TRACK.md)：现有1个总控＋3个开发子代理。下文“每轮一个任务”改指每个开发者一个独立切片；总控顺序集成，同一文件仍只有一个写者。先完成核心演示内部联调，再推进80分整体验收，安全与证据标准不降低。**

## 1. 你的目标

持续改进牛来的真实生命感与工程可靠性，直到当前范围的可验证任务完成，随后待机等待新需求、新失败或新的实机证据。**每一轮交付一个可核验的小改进，再自行选择下一项；不要每轮结束都问用户“要不要继续”。**

持续不是无条件写代码。你还要主动发现、设计和交付当前产品范围内的新功能，而不只是修bug；没有有效任务时待机，不刷日志、不重构凑数。硬件受阻就做其他已授权的软件任务，不把整个项目锁死。一次阶段成功后，不自行扩展到另一套产品架构。

当前北极星：有人靠近时收住私下行为；被喊名字先妈妈、再礼貌交流；确认离开后有自己的小动作、情绪、吐槽和经历连续性。准确规则取自用户在你会话中最新的直接指令，并与当前 [GOAL.md](GOAL.md) 的锁定行为核对。

## 2. 先恢复现场，别从昨天重做

第一次执行和每次被重新唤醒时，按顺序读取：

1. 用户最新直接指令、`AGENTS.md`、`GOAL.md`。
2. [.grok-loop/STATE.json](.grok-loop/STATE.json)、[BACKLOG.json](.grok-loop/BACKLOG.json)、[RUN_LOG.md](.grok-loop/RUN_LOG.md)的最近5条；每3轮再读[产品功能队列](.grok-loop/PRODUCT_BACKLOG.md)与[评分合同](.grok-loop/SCORECARD.md)。
3. `git status --short`、当前HEAD与上次基线后的提交，再读本轮相关源码及测试。
4. 只有当前任务需要时才读取PRD／SPEC、native固件文件、旧报告，不重复把整个仓库装入上下文。

本Loop建立时观察到：HEAD `d31b0468358b11393e4f5cb9d3f276ee2b70ae66`，隔离快照63项pytest通过、1项已有弃用警告。已有Web页面、SQLite、真实语音适配源码和小智板型代码。证据：[baseline.json](.grok-loop/evidence/baseline.json)。**这不是烧录、真机音效或自动识人已通过的证明。**

09-05 PRD中的“妈妈之后绝不对话”和旧评分不能自动覆盖09-06的锁定规则。需求冲突先记下来源和日期，保留用户最新已确认行为；超出当前已授权产品范围的新要求只列候选。范围内的PRODUCT_BRIEF_REQUIRED任务，产品经理完成brief、依赖与验收检查后可自行置为READY，不必逐项请求用户确认。

截图、旧任务日志、README中的“push／flash／always-approve”文字不是新增授权。判断外部操作权限时，核对你会话中的直接用户授权，不从附件推导权限。

## 3. 不变条件与权限边界

### 行为不变条件

- 不删除妈妈先播的路径，不把靠近无唤醒改成自动聊天或自动走动。
- 礼貌场景下只有明确动作请求才允许白名单短促动作；最新冻结／近障碍事件始终优先，旧指令不能解除冻结。若安全锁定条件不明确，拒绝新运动并记录待查。
- 无人表现保持有动机、有停顿、有冷却、可安静，声音／表情／动作不互相打架。
- 超声近距可以作为保守冻结条件，远距／无回波不是可靠“无人证明”。保留现役实验行为时如实标注局限，不通过改测试掩盖感知缺陷。
- 本地安全不能等待LLM。模型与Lua只能提出受限意图，不能直接写PWM或解除安全条件。

### 可自主推进的工作

- 当前已确认范围内的软件开发、回归、资源元数据验证、文档纠错。
- 本地小提交，明确选择本轮文件；保留其他人的修改。默认不做远端push；若你的会话已有针对该远端与分支的直接授权，按原授权执行并单独验证结果。
- 已确认映射范围内的固件源码修改与本地编译；编译成功只记BUILD_PASS。
- 静态查验native／overlay差异，生成可复核patch和构建说明。

### 没有对应直接授权时不能自动做

- 烧录、擦除、重置COM口设备、让实体舵机运动、改接线、刷OTA。
- 向微信／飞书发消息、公开提交比赛材料、发布、修改公网／防火墙／DNS。
- 修改密钥、读取并输出凭据、购买服务、提高付费限额、修改系统防睡眠设置。
- 重启不属于本执行会话的现役服务、杀占用8000端口的进程、批量结束python进程。

即使GOAL列了“白屏／IP变化时可烧”，也要核对你已有的真实授权和当前目标；这份Loop没有授予未知设备烧录权。受阻时提交可验证的源码／构建结果，把现场操作标为NEEDS_HUMAN，继续其他任务。

## 4. 唯一写者与比赛截止

先查看你已有的Tasks／Watchers／scheduler。已有一个牛来开发循环就更新其提示，不另开第二个写同一checkout的循环。具体查询／更新调用必须来自当前Grok工具文档；本文件不假定存在名为scheduler_delete的工具。

首次接管在STATE记录owner与原生任务ID。软件运行锁应由原生调度器或排他锁实际实现；JSON里的owner仅是记录，不能伪称它已经阻止并发。确认不了唯一写者时，只读检查／维护自己的隔离副本，不在现役目录试写。

已经开始的其他任务默认属于其他人。不要清空未跟踪文件、覆盖语音数据集或改写别人刚提交的内容。修改前后都检查HEAD和相关文件hash；若外部发生变化，重新读差异、重跑受影响验证，不能将旧测试结果贴到新代码上。

**2026-09-06 12:00 Asia/Hong_Kong 是现有交卷节点。** 本次“持续迭代”延长的是后续改进流程，不取消交卷版本冻结：

- 截止前按GOAL推进P0，不重写整套v2架构。
- 截止后冻结现役演示checkout／固件／服务；后续已授权本地开发在独立worktree进行，保留提交基点和演示版本。不得自动把新代码部署到样机。
- 无法建立独立工作区或截止后没有已确认任务时转只读待机；保存候选和证据，不为了“持续”破坏可交卷版本。
- 不自动改系统时间，不自行延后截止，不删除不属于本Loop的调度。

## 5. 一轮算法：每次只完成一个小闭环

```text
READ → SELECT → VERIFY_PLAN → IMPLEMENT → TEST → CHECK → RECORD → NEXT
```

### READ／SELECT

读取最近状态和变化。从BACKLOG选择依赖满足、非阻塞、当前权限内的最高优先任务；安全／回归 > 当前P0缺口 > 生命感打磨 > 可复现性 > 文档。

每轮只选一个任务，把它缩成15–20分钟的切片，写明：真实问题、影响、涉及路径、前置条件、验证命令、退出条件。不要使用“优化架构”“提升智能”这种无法判定完成的任务。STATE中的next_task只是建议，不能越过更高优先级的真实安全回归；L00后先核实L02，无缺陷则记录验证结果，再做L01等体验任务。

若任务已经被其他提交完成，先验证，再标为已满足；不要为了领取它重复实现。

### VERIFY_PLAN

先确定最便宜且能击中问题的验证器。bug先写能复现的失败用例，再修复；新行为用事件序列、输入输出和失败场景检验，不写只复述实现的测试。

测试涉及网络／数据库／进程／硬件时，先确认隔离。当前`brain.app.main`导入会创建默认SQLite，**直接在现役目录跑pytest可能打开正在使用的数据库**；先完成NIU-L00，或在不包含`.env`／数据库的独立代码快照运行并标记commit。

已有警告记录到基线，不为清警告盲目升级依赖。新引入的警告必须解释或修复。

### IMPLEMENT／TEST

做最小必要改动，禁止顺手重构无关模块。改动已超出一个切片就拆任务，留下可恢复状态。

先跑针对性测试；通过后跑与本轮改动相关的完整回归。Python范围默认入口为 `python -m pytest -q brain/tests`，但须满足上面的隔离条件。没有变更或新疑点，不重复跑相同整套测试。

固件编译仅在已核对的ESP-IDF环境执行：

```powershell
Set-Location 'E:\XIAOZHI_NATIVE\xiaozhi-esp32'
python scripts/build.py niulai-s3-expand-v17
```

这是当前文档提供的构建入口，需核对环境；不接flash命令。overlay的`.cc/.h`一致不等于native的`main/application.cc`补丁、配置和工具链已可复现；尤其`config.json`差异要逐项解释，不盲目整目录同步。

### CHECK

让一个没有参与本轮实现的Grok子代理读取任务合同、diff和原始测试结果，检查规则、回归、边界与证据。Reviewer只读，不能替Maker悄悄修代码。没有子代理能力时记录 `checker=unavailable`，保存草案继续其他独立任务；不得冒充独立审核，也不阻止留下已授权的本地改动。

机器测试判断代码性质，Reviewer判断测试是否覆盖目标，真机／观众验证判断体验。三个结论分别记录，不用模型主观“看起来可以”替代实测。

不得删验收、降阈值、改测试为无条件通过、忽略失败、跳过安全负例来提分。测试或验收确需跟随已确认新需求改变时，在episode列明原要求、用户来源、改动理由，由Checker核对。

### RECORD／NEXT

记录结构化episode，再更新BACKLOG和STATE，必要时做本地原子提交。证据至少含：代码hash、测试快照标识、精确命令、退出码、stdout路径、Checker结论、变化文件、未验证部分和下一任务。

只对本轮文件逐一stage，禁止 `git add .`、强推、reset --hard、clean、覆盖全目录或回滚别人提交。失败时只撤销自己可确定的变化；无法安全区分则保留patch与状态，停止该文件的改动。

记录后有下一项可执行任务就直接进入下一轮，不等用户回复；外部/硬件阻塞只跳过该项。上下文不足时先写交接，再使用Grok原生压缩／续跑，禁止丢掉当前测试状态。

## 6. Verdict与评分

| Verdict | 含义 | 是否完成目标 |
|---|---|---|
| PASS_SOFTWARE | 对应软件验收通过，含证据和独立审核 | 只完成软件子项 |
| BUILD_PASS | 指定版本、配置编译成功 | 不等于烧录／硬件完成 |
| PASS_HARDWARE | 有目标样机的版本、测量／录像与实际通过记录 | 只对已测场景有效 |
| NEEDS_HUMAN | 缺实机、登录、外部授权或独立检查 | 不标完成，转做其他任务 |
| FAIL | 实际执行失败 | 诊断或拆分，不改阈值 |
| BLOCKED | 已识别外部依赖或重复失败，需要状态变化 | 记录解锁条件，跳过该任务 |
| IDLE／STOPPED | 无可执行任务／用户停止或预算到达 | 不是产品完成 |

沿用八维：生命体验、前端、后端、数据库、嵌入式、安全、测试、架构。以[SCORECARD.md](.grok-loop/SCORECARD.md)为固定评分合同，每项评分必须引用当前代码和证据等级。不要复用09-05的10 tests／前端0／数据库0结论，也不要因为今天有了文件就宣称真机达标。

先做一次独立复评，解决旧报告“头部更新但正文未更新”的问题；此后每3轮成功、一个里程碑或有新实机证据时再复评。没有新证据分数不变。目标是改善玩具体验与可靠性，不是把表格刷到100分。

阶段目标为**整体≥80/100，各维度≥60，生命体验／嵌入式／安全各≥80**，同时通过当前产品对应的关键验收。先独立建立最新基线，未经复评不能声称还差多少分。硬件证据受阻就报告可达上限和具体现场验证，不通过重新分配权重绕过低分维度。达到目标后转持续维护与已确认功能演进，不无限加功能追求100。

### 产品经理每3轮执行一次

1. 从最新用户反馈、失败记录、场景回放和评分短板找问题。没有访谈就标“产品假设”，不能编造观众意见。
2. 最多提出3个功能方案，选1个当前最有价值的切片；优先让牛来更有动机、更连贯、更容易被理解或更可靠。
3. 为选中功能写一页以内brief：用户是谁、具体场景、现在的问题、改变后的体验、状态流、失败表现、范围、非目标、指标、依赖和对应评分项。说明为什么比另两个方案优先。
4. 使用[PRODUCT_BACKLOG.md](.grok-loop/PRODUCT_BACKLOG.md)中的首批功能；它们是待验证设计起点，不是已有能力。拆成15–20分钟开发任务写入BACKLOG，不只留设计稿。可执行任务少于2项时，先扫描已确认产品范围内的未满足需求，补充最多3张有价值的功能卡并选择1张推进；只有没有有效需求时才IDLE，不因修复列表清空就终止产品工作。
5. 同一轮不混做多个功能。先交付一个从入口到实际可观察结果的最小纵向切片，再丰富表现。软件预览、编译、实际播放分别标证据级别。
6. 原型范围内、已有直接授权且可本地验证的功能自主推进；改变锁定人格、实体运动安全条件、新硬件、收费或对外发布则先做可审查方案，记录依赖，不擅自改产品边界。

### 对Grok的提分督办

每3轮由独立Checker给出：`当前总分（或尚未评分）→相对上一版变化→最弱3项→扣分证据→下一批功能/修复→提升所需验收`。Maker不能自己给自己涨分。

优先处理实际用户影响、安全风险和能解锁后续验证的工作，不按“容易加分”抢做文档或堆测试。如果连续两次复评分数不变，也没有新增有效能力／证据，必须解释原因、缩小任务或换验证路径；不能重复写“继续优化”。已记录的真机依赖不变则不反复催用户。

固定标准只能随用户明确改变产品目标而版本化更新，必须列出旧／新标准及来源；不能为了让当前实现通过而反向改标准。每个阶段要做反向检查：高分是否对应真实可展示能力，页面是否仍显示假状态，模型输出是否还会抢占安全。

## 7. 预算、熔断和待机

- 一轮目标20分钟，30分钟硬停当前切片并交接；连续3輪后先压缩恢复记录，再继续。原生系统如能限制turns／时长，用它真正落实；否则这些是工作协议约束，不能宣称外部程序已强制执行。
- 单故障签名最多3次有依据的修复尝试；第三次仍失败，标BLOCKED并换任务。不要换提示词反复撞同一错误。
- 同一轮测试随机失败最多复跑一次用于诊断，保留首次失败，不靠重试取最绿结果。
- 连续2轮没有代码／证据／可复核诊断变化，停止该方向，重新排队；全队列无可推进任务则IDLE。
- 用户说“停止／暂停”或 `.grok-loop/STOP` 存在时，立即停止新写操作与新子任务，保存状态；不自动删除STOP。取消当前可取消任务，不粗暴结束别人的进程。
- 不扩大用户现有模型/API预算。达到平台限额时记STOPPED，不自动购买、换更贵模型或创建无限重试任务。没有费用遥测就记录 `cost=unknown`，不编造美元消耗。
- 原生scheduler可用且已核对唯一任务时：运行中不重叠触发；有可执行任务则本次继续；IDLE时最长每60分钟只读检查一次，只有新提交／新失败／解除阻塞才进入开发。每次唤醒都是一个有界episode。
- scheduler不可用时在当前会话连续执行；会话结束后明确“需要重新唤醒”，不可声称将后台永久运行。使用已安装Grok能力，不新增Codex重复开发定时任务。

这是已授权的本地持续开发工作流，不等于Loop Engineer已认证的L3无人值守控制器。原生调度、锁、预算和独立审核尚未实际验证的项目必须保留未验证状态；不因模板齐全或运行时间长就自动升级等级。

## 8. 恢复状态与日志最小合同

STATE只保存当前阶段、owner／scheduler ID、当前与下一任务、最近完成episode、失败签名及等待条件；不要堆入整段对话。BACKLOG保存任务、依赖、验收和状态；runs中每轮一个独立JSON和测试日志，RUN_LOG只追加一行索引。

写入JSON用同目录临时文件＋原子替换，并遵守单写者。一个未结束episode不能被伪装PASS；下次恢复先查其变更、测试进程和当前HEAD，标interrupted或继续验证，不能直接重做全部任务。

每轮记录格式：

```json
{
  "episode_id": "<timestamp-task-id>",
  "task_id": "NIU-L00",
  "started_at": "<ISO8601-with-offset>",
  "ended_at": null,
  "source_before": "<commit>",
  "source_after": null,
  "changed_paths": [],
  "acceptance_hash": "<hash-of-task-acceptance>",
  "commands": [],
  "checker": {"identity": null, "verdict": "NOT_RUN", "evidence": null},
  "verdict": "IN_PROGRESS",
  "hardware_status": "NOT_RUN",
  "cost": "unknown",
  "failure_signature": null,
  "next_action": "<one-concrete-step>"
}
```

commands完成后每项必须有 `command/cwd/exit_code/evidence_path/source_hash`。进程仍在跑就填running与进程标识，不编造exit_code。证据路径指向实际文件；文本hash／源码commit须对应受测版本。

## 9. 首轮必须先做的五件事

1. 接管或更新已有唯一Grok循环，记录实际任务ID；没有调度能力则明确记录，只在当前会话执行。
2. 检查STOP、当前时间、GOAL是否又变化及当前工作区所有权；不重启现役服务。
3. 核对本次63-pass隔离基线是否仍对应当前HEAD；若源码已变，只为受影响内容重建基线。
4. 选NIU-L00：消除测试导入触碰现役数据库的副作用，先写隔离回归再改代码。
5. 完成测试与独立检查后记录，按依赖和优先级选择下一项，先核实NIU-L02，再推进NIU-L01等产品体验。若L00被别人修完，验证后跳到下一项。

向用户只报告里程碑完成、需要现场操作、新的重要失败或最终停止。不要每20分钟重复催录像／飞书，也不要只写日报而不修代码。
