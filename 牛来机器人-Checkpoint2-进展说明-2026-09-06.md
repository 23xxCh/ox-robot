# 牛来机器人 · Checkpoint 2 进展说明

| 项 | 内容 |
|---|---|
| 项目名称 | 牛来（潮玩生命玩具 / ox-robot） |
| 日期 | 2026-09-06 |
| 节点 | Checkpoint 2 = 9.6 10:00–10:30；Submission = 9.6 12:00 |
| 交卷文件 | [牛来机器人-Submission-2026-09-06.md](./牛来机器人-Submission-2026-09-06.md) |
| 代码仓库 | https://github.com/23xxCh/ox-robot |
| 飞书提交稿 | https://my.feishu.cn/wiki/Afn3w2nH8iuzd6kMbcEc0NY8nBf |
| 团队 | 陈熙贤（硬件）· 潘炜德（软件）· 微微（产品 & 商业） |

一句话：人前它是一只只会说「妈妈」的机械玩具；人离开约 8 秒后，它才敢自己晃一晃。冻结走板端 C，不走大模型，不连小智云。

---

## 1. 相对 Checkpoint 1，新完成的部分

### 1.1 板端生命循环（本轮核心）

- 板型 `niulai-s3-expand-v17` 已烧进 COM6。最近一次：2026-09-05 19:06（GPIO 10/11/9/13），hash verified，RTS 复位。喇叭音量强制 90。
- 超声 GPIO8/17：近距（约 55 cm 内）**立刻**四腿 1500 µs 中位，并播本地「你回来啦」。不把超时当成「确认无人」。
- 约 8 秒没有近距才进入 SECRET：随机步态小晃约 800 ms（硬帽 1100–1900 µs），随后约 2 s 停在 1500 µs，并轮播三句本地吐槽。再靠近立刻停。喊「妈妈」/ BOOT / 键也会 `ParkActuators()`。
- 屏上是黄牛来脸（灰角、紫鼻子、半眼皮），不是小智机器人图标。
- 喊「牛来牛来」/ 按 KY-004 / BOOT：只播本地 `mama.ogg`（Opus），跳过官方 OTA，拒绝官方 websocket。

### 1.2 本地大脑（笔记本，本轮不假装已接通真机 TTS）

- FastAPI `brain/`：人格 MECHANICAL/SECRET、Lua `niu.*`、MCP `ttl_ms ≤ 2000`、飞书/微信 `event_id` 去重、SQLite、排练页、公开 `/life` 人设页。
- `/life` 可在笔记本上写人设、用 DashScope Qwen 兼容模式对话。这不是小智云，也不驱动真机冻结/舵机。无 key 时测例仍绿。
- 协议测例可重复跑。真机吐槽是板上的 `hi.ogg` / `secret*.ogg`。路演不演 /life 闲聊。

### 1.3 机械与接线

- 打印包仍是 v0.5。现场用开框/面包板能站上桌即可。
- 灯 S=GPIO14，键 S=GPIO18。屏 SCL=21、MOSI=47，不能再占。左后舵机 **GPIO 9**；GPIO 12 是板上充电指示 CHRG，PWM 会打架。

---

## 2. 还剩什么（到 12:00 交卷）

| 序号 | 事项 | 是否阻塞 90 秒演示 | 说明 |
|---|---|---|---|
| 1 | ≤3 分钟连续真机录像 | 交卷需要，路演可现场演 | 拍：妈妈 + 靠近「你回来啦」+ 离开小晃和本地吐槽 |
| 2 | 外壳总装 / 真正走路闭环 | 不阻塞 | 不演跑步 |
| 3 | /life LLM 接到真机喇叭 | 不阻塞 | 笔记本可聊人设；90 秒只演板端本地录音 |
| 4 | 灯绿/蓝、毫米波、第二块板 | 不阻塞 | 领了但本轮不用 |

不做什么：不合完整 ESP-Claw+小智；不用 ESP 的 5V 喂舵机；freeze 不交给大模型；不打开 xiaozhi.me 升级。

---

## 3. 新遇到的难点

1. **官方 OTA 会覆盖拦截。** 配过小智云后，官网 websocket/OTA 会把「只说妈妈」冲掉。现固件启动跳过 OTA、拒绝打开官方音频通道。不要再进 xiaozhi.me 点升级。
2. **屏脚被占。** GPIO 21/47 是 ST7789 SPI。灯/键必须 14/18。
3. **舵机种类外观一样，且 GPIO 12 是充电指示。** 没给 1.5 ms 中位时，360° 会一直转。现固件 10/11/**9**/13 保持中位；SECRET 随机步态晃约 800 ms 再停。黄线接 12 会跟 CHRG 打架。
4. **HC-SR04 ECHO 是 5V。** ESP32-S3 GPIO 不耐受 5V。现场若 ECHO 直连，建议 1k+2k 分压；固件把超时当「没有近距」，不当成身份识别。
5. **虚拟 `SetupUI` 不会派发到子类。** 牛脸在 `NiulaiLcdDisplay` 构造函数里、基类 UI 建完之后再画，否则屏上仍是小智图标。
6. **Vorbis 的 ogg 喇叭无声。** 小智 `PlaySound` 只吃和官方铃声同款的 Opus。`mama.ogg` / `hi.ogg` / `secret*.ogg` 已转成 48 kHz Opus。

---

## 4. Checkpoint 2 巡场 60–90 秒

1. 指黄牛脸：「有人时是玩具。」
2. 喊「牛来牛来」或按键 → 「妈妈」。说明：本地录音，不经小智云。
3. 手靠近超声 → 腿立刻停，喇叭说「你回来啦」。
4. 手拿开等约 8 秒 → 腿随机小晃，说一句本地吐槽。再靠近立刻停。
5. 打开仓库。不演 /life 闲聊，不演跑步，不连小智云。

---

## 5. 分工

| 人 | Checkpoint 1 | Checkpoint 2 / 交卷 |
|---|---|---|
| 陈熙贤 | 接线、供电、舵机外供 5V、超声、开框 | 现场确认灯 14 / 键 18、拍 3 分钟录像、外壳尽量站上桌 |
| 潘炜德 | 板型 overlay、本地妈妈、brain 测例 | 生命循环固件、拒绝小智云、Submission 文档、仓库 |
| 微微 | 飞书叙事、场景与商业 | 交卷清单、路演词、诚信声明 |

Checkpoint 加权仍是：进展 40% · 技术可行性 20% · 问题与场景清晰度 20% · 基础安排/分工 20%。
