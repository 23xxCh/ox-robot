# 牛来机器人参考调研：MicroDuck 的架构与工作原理

> 调研日期：2026-08-31  
> 调研对象：Pollen Robotics / Hugging Face MicroDuck  
> 结论来源：官方产品页、官方运行时仓库、官方强化学习仓库及其架构设计文档。

## 1. 一句话结论

MicroDuck 不是“ESP32 玩具接一个大模型”，而是一台约 25 cm、800 g 的 Linux 双足机器人：Rockchip RK3566 单板机在机身内运行 Rust 服务，使用 50 Hz 控制循环读取 15 个智能舵机和 IMU，将 61 维机器人状态送入 ONNX 神经网络策略，再把 14 维动作转换成关节目标并经过安全层写回舵机。

它真正值得牛来借鉴的不是“双足强化学习”本身，而是四条工程原则：

1. 上层只提交“意图”，不能直接写电机。
2. 安全层独占执行器写权限。
3. 行为策略可以替换，传感器、控制周期和安全合同保持稳定。
4. 先在数字孪生中训练/测试，再部署到真机，并保留回滚能力。

## 2. 产品和硬件概况

官方公开的整机信息：

| 项目 | MicroDuck |
|---|---|
| 形态 | 小型双足鸭形机器人 |
| 高度/重量 | 约 25 cm / 800 g |
| 主计算 | Rockchip RK3566 Linux 单板机，当前软件针对 Radxa 平台 |
| 执行器 | 15 个 Dynamixel 智能舵机 |
| 策略控制自由度 | 14 个；嘴部舵机不进入运动策略 |
| 传感 | 摄像头、头部 8×8 ToF/LiDAR、双 IMU、舵机位置/速度/电流/电压/温度 |
| 音频 | 板载麦克风、扬声器和音频编解码器 |
| 电池 | NP-F550 形态的 2S 锂电池；运行时按 6.6–8.2 V 可用区间估算电量 |
| 通信 | Wi-Fi、蓝牙、USB/SSH、手柄、WebRTC |
| 控制频率 | 50 Hz，即每 20 ms 一次控制周期 |

### 2.1 关节布局

```text
左腿 5：髋偏航、髋横滚、髋俯仰、膝、踝
头部 5：颈俯仰、头俯仰、头偏航、头横滚、嘴
右腿 5：髋偏航、髋横滚、髋俯仰、膝、踝
```

15 个舵机和 `imu_to_dxl` 板共享一条 UART，使用 Dynamixel Protocol 2.0、1 Mbps。IMU 板使用单独设备 ID，但与舵机在同一批次读取。

## 3. 整体软件架构

MicroDuck 采用“单板多守护进程 + 单一实时控制核心”的模块化架构，而不是 ROS 2。

```text
       手柄           手机/BLE         浏览器/远程          CLI/SSH
        │                 │                │                  │
      padd               btd            mediad            robotctl
        └─────────────── 高层意图 / JSON-RPC ────────────────┘
                                  │
                         Unix Domain Sockets
                                  │
                    ┌─────────────▼─────────────┐
                    │ robotd：唯一运动控制核心 │
                    │ 50 Hz + 策略 + 安全层     │
                    └─────────────┬─────────────┘
                                  │ Dynamixel UART
                        15 舵机 + IMU 转接板

 configd：Wi-Fi、身份、配对、电源
 updaterd：签名更新、健康门禁、自动回滚
 tofd：8×8 ToF 深度流
 mediad：摄像头/麦克风、WebRTC、视觉推理
 btd：蓝牙传输适配
 padd：手柄输入适配
```

### 3.1 为什么拆成多个进程

- `robotd` 只负责运动控制、安全、策略和机器人状态。
- 摄像头、媒体和视觉推理崩溃，不能拖死电机控制。
- 即使 `robotd` 起不来，`configd`、`updaterd` 和 `btd` 仍能联网、重新配置或回滚。
- 每一类状态只有一个写入者，避免多个服务同时控制同一硬件。

### 3.2 进程通信

- 控制面：JSON-RPC 2.0 + NDJSON，运行在 Unix Socket 上。
- 数据面：摄像头/音频不塞进 JSON-RPC，单独走媒体管线和 WebRTC。
- 控制循环不做同步远程调用，跨服务数据采用缓存/最后值优先，防止阻塞 20 ms 周期。
- 手机、手柄、浏览器和 CLI 最终都发送同一套“意图”，不会各自拥有一条电机控制后门。

## 4. 最核心的工作原理：50 Hz 神经网络控制环

每 20 ms 执行一次：

```text
1. sync_read
   读取 IMU + 15舵机位置/速度/电流等
          │
          ▼
2. 构造 61维 Observation
          │
          ▼
3. 根据高层意图选择 ONNX Policy
   walk / stand / sit / kick / ground-pick / roulade...
          │
          ▼
4. ONNX 推理输出 14维 Action
          │
          ▼
5. 动作缩放 + 低通滤波 + 回填到15关节
   嘴部不受策略控制
          │
          ▼
6. Safety.apply
   NaN拒绝、关节限位、失联停车、跌倒处理
          │
          ▼
7. sync_write
   一次性写入全部目标关节角
```

### 4.1 61 维输入是什么

| 范围 | 数量 | 内容 |
|---|---:|---|
| 0–2 | 3 | IMU 角速度 |
| 3–5 | 3 | 机身坐标系重力方向 |
| 6–19 | 14 | 关节位置减去默认站立姿态 |
| 20–33 | 14 | 关节速度 |
| 34–47 | 14 | 上一时刻策略动作 |
| 48–60 | 13 | 速度、头部姿态和身体姿态命令 |

合计 `3 + 3 + 14 + 14 + 14 + 13 = 61`。

### 4.2 14 维输出是什么

神经网络输出14个连续值，对应左右腿和颈/头关节。运行时把它们乘以动作比例、叠加默认姿态，再经过低通滤波生成目标角度。第15个嘴部舵机被明确排除，因此嘴巴可以由声音/表情逻辑独立控制。

### 4.3 为什么不是直接让大模型控制舵机

LLM 反应慢、输出时间不确定，也无法稳定满足 20 ms 控制周期。MicroDuck 的客户端只能发送：

- 前进速度、横向速度、转向速度；
- 头部朝向；
- 站立、坐下、踢球、翻滚等技能意图。

`robotd` 再选择经过训练和验证的策略。这个模式与牛来设计中的 `Action Gateway + Safety Supervisor` 是同一种安全思想。

## 5. 强化学习和 Sim-to-Real 原理

MicroDuck 的走路不是人工编写每条腿的轨迹，而是在仿真中学习出来的。

```text
Onshape/CAD
    ↓ 导出 MJCF
MuJoCo Warp / mjlab 物理仿真
    ↓ 4096个并行环境
PPO 强化学习
    ↓ PyTorch checkpoint
导出 ONNX（包含 Observation Normalizer）
    ↓
真机 robotd / ONNX Runtime / 50 Hz
```

### 5.1 策略怎么学会走路

训练程序随机给机器人目标速度，奖励包括速度跟踪、站稳、姿态、能耗和动作平滑等；摔倒、动作突变或偏离目标则受到惩罚。PPO 不断更新神经网络，使累计奖励提高。

官方仓库给出的参考量级是：4096 个并行仿真环境训练可用步态约需 1–2 小时，前提是有 CUDA GPU。

### 5.2 为什么仿真里学会，真机也能走

关键不是单纯“加随机噪声”，而是针对真实执行器建模：

- 使用 BAM 的 Dynamixel XL330/M6 电机模型；
- 模拟反电动势、摩擦、负载相关摩擦；
- 随机化电池电压和负载压降；
- 随机化命令延迟；
- 随机化重心、质量和接触条件；
- 额外建立每个舵机约 ±1° 齿轮间隙版本；
- 训练和运行保持相同的动作缩放、低通滤波和 61维输入布局。

换句话说，Sim-to-Real 的核心是让仿真“像一批略有差异的真机器人”，而不是让策略只适应一个完美模型。

### 5.3 多策略切换

走路、站立、坐下、踢球、捡东西、前滚翻和轮滑不是一个万能模型，而是多个相同输入/输出合同的 ONNX 策略。运行时根据意图和优先级切换策略；因为接口统一为 `61 → 14`，切换时不用改控制总线。

## 6. 安全架构

MicroDuck 最重要的安全措施不是某一个传感器，而是“唯一写权限”。

### 6.1 执行器单一所有者

`Safety` 对象持有唯一的 `RobotIo`，策略、控制器和网络客户端只能提出目标。Rust 所有权/借用机制在编译期阻止其他模块直接写舵机，而不仅依靠团队约定。

### 6.2 运行时保护

- 非有限动作（NaN/Inf）直接拒绝，保持原姿态；不能简单 clamp 成一个看似合理的边界值。
- 超出关节范围的目标被截断，并在状态中报告原因。
- 速度意图超过约 500 ms 没刷新，速度归零，但机器人继续站立，不直接瘫软。
- 传感器读取失败时不伪造 Observation，也不继续让策略按旧状态驱动。
- 电池耗尽时先执行安全动作，再关机。
- 电机温度、电压、总线错误和控制周期均可观测。

### 6.3 跌倒处理

它有两个不同的判断：

1. “已经倒下”的姿态判定，用于报告状态；
2. “正在倒下”的预测器，根据陀螺仪与重力方向估计约 300 ms 后的姿态。

预测确认后，系统降低舵机刚度让机器人软着陆；检测到停止滚动后，把关节缓慢拉回已知姿态，再交给站起策略。这样避免机器人倒地后仍用高刚度反复挣扎。

## 7. 更新和恢复架构

- 整套服务按版本目录安装，不在当前版本上原地打补丁。
- 发布包经过签名验证。
- 切换 `current` 版本后重启服务并调用健康检查。
- 控制循环、总线或策略加载不健康时自动回滚到上一版。
- 单板配置和身份存放在发布目录之外，因此升级/回滚不会覆盖机器专属配置。

这套机制对黑客松 P0 太重，但“保留最后可工作的固件 + 每次改动跑完整演示回归”值得牛来直接采用。

## 8. 它现在有没有“大模型灵魂”

没有。官方当前主运行时的核心是运动策略、传感器、声音、媒体、联网、更新和遥控，并没有把 LLM 当作自主大脑。

官方规划中的自主行为栈尚未正式进入主守护进程。设计草案描述了16个状态：放松、环顾、游荡、原地转向、疯跑、受惊、伸展、整理羽毛、打喷嚏、跳舞、捡东西、睡觉、玩球、被抚摸、被抱起等；其选择由能量、情绪、新奇度地图、声音、ToF和社交输入共同决定。

所以要区分：

- MicroDuck 已经实现得很强的是“身体运动能力”；
- 规划中的状态机负责“像活物一样自主活动”；
- LLM/MCP 只是社区可扩展的高层入口，不是 50 Hz 身体控制器。

## 9. 对牛来机器人的可借鉴性

### 9.1 建议直接借鉴

| MicroDuck 做法 | 牛来对应实现 |
|---|---|
| 客户端发送 Intent | ESP-Claw/Lua 只发送语义 `ActionIntent` |
| `robotd` 独占电机 | `Safety Supervisor + Motion Controller` 独占 PWM/H桥 |
| Safety 持有唯一 RobotIo | HAL 电机写接口只对安全层私有可见 |
| 50 Hz 有界循环 | 牛来运动/安全任务固定周期，禁止网络阻塞 |
| Deadman | 动作租约到期自动停车 |
| FakeIo | 雷达、超声波、电机和舵机都提供 Mock |
| 统一 Observation/Action 合同 | 统一事件、状态快照和命名动作合同 |
| 多策略同接口切换 | 双人格状态机在同一动作集合上编排 |
| 仿真/实物一致参数 | FreeCAD 包络、质量、重心和动作限位进入测试基线 |
| 签名更新和回滚 | 黑客松阶段至少保存稳定固件、配置和回归结果 |

### 9.2 不建议照搬

1. **不照搬双足强化学习。** 牛来当前是差速轮移动，规则控制即可；引入 MuJoCo + PPO 会把机械、电机和训练风险同时放大。
2. **不照搬 Linux 七进程。** ESP32-S3 资源和系统模型不同，P0 使用 FreeRTOS 模块化单体更合适。
3. **不购买15个智能舵机来追求同样动作。** 成本、重量、供电和机械难度都会失控。
4. **不把 LLM 放进实时环。** LLM 负责台词与高层意图，安全与动作始终本地确定。
5. **不宣称 MicroDuck 的自主灵魂已经量产成熟。** 官方文档明确仍把 autonomous brain 作为后续工作。

## 10. 对牛来架构的实际调整建议

现有牛来“双平面架构”方向正确，不需要推翻：

```text
ESP-Claw / LLM / 人格状态机
           │ 语义动作
           ▼
Action Gateway
           │ 有时限的动作租约
           ▼
Safety Supervisor
           │
Motion / Servo / Audio / Display
```

受 MicroDuck 启发，建议补强四点：

1. 将电机/舵机唯一写权限落实到代码可见性，Agent、Lua和状态机都不能获得底层驱动句柄。
2. 建立固定 `RobotSnapshot`：presence、obstacle、battery、motion、servo_pose、faults、age。
3. 建立 `FakeRobotIo` 场景测试，在硬件到货前回放“突然有人、传感器失联、动作过期、低电、急停”。
4. 把“灵魂”实现成能量/情绪/新奇度驱动的本地状态机；LLM只为状态机生成候选台词，不负责决定电机 PWM。

## 11. 结论

MicroDuck 的核心竞争力可以概括为：

> 用高保真仿真和强化学习解决双足运动，用严格的软件边界和安全层把神经网络约束在可控范围内。

牛来不需要复制它昂贵、复杂的双足身体，但非常适合复制它的“上层意图—策略/状态机—安全层—唯一硬件写入口”结构。这会让牛来既能表现出自由人格，又不会因为 AI 输出或网络延迟失控。

## 12. 官方资料

- [MicroDuck 官方产品页](https://pollen-robotics.com/microduck)
- [MicroDuck 官方运行时仓库](https://github.com/pollen-robotics/microduck)
- [MicroDuck 系统架构](https://github.com/pollen-robotics/microduck/blob/main/docs/design/architecture.md)
- [`robotd` 50 Hz 控制架构](https://github.com/pollen-robotics/microduck/blob/main/docs/design/robotd-design.md)
- [运动模型与关节定义](https://github.com/pollen-robotics/microduck/blob/main/duck-control/src/model.rs)
- [61维 Observation 定义](https://github.com/pollen-robotics/microduck/blob/main/duck-control/src/obs.rs)
- [MicroDuck RL 官方仓库](https://github.com/pollen-robotics/microduck_rl)
- [自主行为栈草案](https://github.com/pollen-robotics/microduck/blob/main/docs/ideas/autonomous_behavior.md)

## 13. 调研边界

- 官方 `architecture.md` 和 `robotd-design.md` 当前标记为 draft，产品仍处于快速迭代期。
- 本文只把官方仓库和产品页作为确定性事实来源；社区复刻、MCP项目和第三方推测未用于硬件结论。
- 软件代码使用 Apache-2.0；强化学习仓库注明3D模型文件采用 CC BY-SA-NC，二次使用模型前需单独检查资产许可。
